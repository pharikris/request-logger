pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t pharikris/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag pharikris/request-logger:${env.BUILD_ID} pharikris/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push pharikris/request-logger:${env.BUILD_ID}"
        sh "docker push pharikris/request-logger:latest"
      }
    }
  }
}
